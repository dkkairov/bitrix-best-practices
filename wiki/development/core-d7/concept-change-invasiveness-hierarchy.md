---
title: "Иерархия способов изменения: от штатного механизма до копии в /local/"
type: concept
module: core-d7
edition: box
status: verified
provenance: documented
verified: "2026-09-21 / «Книга разработчика Bitrix24» (bx24devbook, снимок 2026-09-21): Введение в разработку"
tags: [разработка, стандарт, ядро, local, расширение, манифест]
sources: ["[[source-devbook-dev-rules]]"]
related: ["[[concept-dev-standards]]", "[[antipattern-box-core-modification]]", "[[pattern-events-over-core-modification]]", "[[concept-platform-reverse-engineering]]", "[[pattern-local-solution-structure]]"]
aliases: ["bitrix24-pravila-razrabotki"]
updated: "2026-09-21"
---

# Иерархия способов изменения

**TL;DR:** любую доработку коробки начинаем с верхнего уровня и спускаемся вниз, только если
предыдущий не подходит. Чем ниже уровень — тем дороже обновления продукта.
Источник — [«Введение в разработку»](https://bx24devbook.website.yandexcloud.net/Razrabotka/Vvedenie.html)
«Книги разработчика» (сверено 2026-09-21; прежняя ссылка на dev.1c-bitrix.ru была ошибкой переноса).

## Зачем эта страница

Главная беда платформы — **отсутствие ограничений**: технически «можно всё», включая правку файлов
продукта. Поэтому нужен явный кодекс, а не только запрет «не трогай ядро». Эта страница — рамка
принятия решения; конкретика по каждому уровню — в связанных страницах.

## Пять уровней (сверху вниз)

| # | Уровень | Когда применять | Пример |
|---|---------|-----------------|--------|
| 1 | **Штатный механизм** | Задача укладывается в готовый функционал продукта | Генерация PDF — встроенным модулем, а не своим |
| 2 | **Легитимное расширение** | Штатного не хватает, но есть точка расширения | Обработчик события ([[pattern-events-over-core-modification]]), подписка на JS-события страницы, `Operation\Action` для CRM ([[pattern-crm-action-vs-event]]), свой тип UF-поля, своё действие БП ([[recipe-bizproc-custom-task-activity]]) |
| 3 | **«Мягкое» изменение** | Нужно подкрутить отображение/поведение | JS/CSS-скрытие ([[recipe-crm-hide-card-block-js]]), `result_modifier.php`, `component_epilog.php` |
| 4 | **Параллельная разработка** | Штатное сильно расходится с нужным | Своя механика рядом, штатная скрыта через JS/CSS |
| 5 | **Копия в `/local/`** | Всё остальное не подошло | Перенос компонента из `/bitrix/components/bitrix/` в `/local/components/…`. **Последний вариант** |

Правило перехода вниз: спускаемся на уровень ниже, только когда доказано, что верхний не решает
задачу — а не когда «так быстрее».

## Жёсткий список «никогда»

1. Менять `/bitrix/components/bitrix/*`
2. Менять `/bitrix/modules/*`
3. Менять `/bitrix/php_interface/*` (вместо — `/local/php_interface/`)
4. Менять `/bitrix/js/*`
5. Менять `/bitrix/css/*`
6. Ручная переиндексация правил ЧПУ (`urlrewrite.php`)
7. Визуальный редактор для правки компонентов
8. `INSERT` / `UPDATE` / `DELETE` в БД напрямую

Разбор последствий — [[antipattern-box-core-modification]].

**Часто избегать:** работы без SSH и актуального бэкапа; прямых `SELECT` в обход ORM. Осознанное
исключение — чтение данных, которые ядро само считает SQL-запросами (например,
[[pattern-tasks-effectiveness-from-db]]): там прямой `SELECT` повторяет запросы ядра, пишем только
через API.

## Как правильно (пять практик)

1. **VCS** для всего кода доработок — [[checklist-dev-environment-and-git]]
2. **Работа только в публичной части и `/local/`**
3. **Песочница**: минимум две среды (боевая + dev), перенос автоматическими средствами. На боевом
   сайте правят только контент. Для тестовой копии в книге упомянут параметр «Установка для
   разработки».
4. **Перенос изменений миграциями/инсталляторами** — [[recipe-migrations-as-code]]
5. **Учётные данные в `.env` и в БД (опции модуля, `Option`)**, не в коде — как держать `.env`,
   см. [[pattern-local-solution-structure]]

## Принципы мышления

- **Сначала формализуй требование в терминах Bitrix24** — возможно, нужное уже есть в продукте.
  Это же экономит уровень 1 в таблице выше.
- **Думай о будущих сценариях, а не только о текущих.** Книга напоминает: официальной документации
  по коду нет, обратная совместимость на уровне кода не гарантируется.
- **Найди все точки воздействия на сущность.** «Изменение лида» — это карточка, список, канбан,
  REST, обработчики событий, бизнес-процессы. Правка в одном месте почти никогда не достаточна.
- **Не понимаешь, как работает штатное — не меняй его.** Дешевле написать своё рядом, чем чинить
  чужую логику вслепую. Если понять всё-таки надо — [[concept-platform-reverse-engineering]].

## Кажущееся противоречие про `php_interface`

Книга одновременно запрещает менять `/bitrix/php_interface/` и описывает `init.php`,
`after_connect.php`, `dbconn.php` как части пайплайна страницы
([Ядро продукта](https://bx24devbook.website.yandexcloud.net/Obsie_svedenia/Adro_produkta.html#fajlovaa-struktura),
[Страница](https://bx24devbook.website.yandexcloud.net/Obsie_svedenia/Stranica.html#poradok-vypolnenia)).
Резолюция: «штатные» — про **назначение** каталога; правильная **реализация** — параллельный
`/local/php_interface/`, который имеет приоритет и не затирается обновлением. Оговорка: по порядку
выполнения из книги приоритет `/local/` подтверждён только для общего `init.php`; у `dbconn.php` и
`after_connect*.php` аналога в `/local/` нет — [[entity-php-interface]].

## Связанные страницы
- [[concept-dev-standards]] — общий стандарт разработки коробки
- [[antipattern-box-core-modification]] — что происходит при нарушении
- [[pattern-module-based-development-standard]] — «модулем или нет» как первый вопрос задачи

[← Ядро D7](_index-core-d7.md)
