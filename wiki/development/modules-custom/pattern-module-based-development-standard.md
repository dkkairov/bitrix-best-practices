---
title: "Модульная разработка: когда и как"
type: pattern
module: modules-custom
edition: box
status: verified
provenance: mixed
verified: "2026-06-21 / Bitrix Framework (курс 43); 2026-09-21 / сверено с «Книгой разработчика Bitrix24» (bx24devbook): Свой код — «Решение vs модуль»"
tags: [модуль, local, дистрибуция, переиспользование, коробка, решение]
sources: ["[[source-bxfw-course43-modules]]", "[[source-devbook-dev-rules]]"]
related: ["[[recipe-module-structure-and-install]]", "[[recipe-module-versioning-and-private-distribution]]", "[[pattern-module-library-monorepo]]", "[[antipattern-box-core-modification]]", "[[pattern-local-solution-structure]]"]
aliases: []
updated: "2026-09-21"
---

# Модульная разработка: когда и как

> **Сверено с «Книгой разработчика» 2026-09-21.** Книга смотрит на Bitrix24 как на монолит под
> конкретного клиента: по умолчанию клиентский код — «решение» в `/local/php_interface`, модуль —
> только для действительно переиспользуемого кода
> ([Свой код → Решение vs модуль](https://bx24devbook.website.yandexcloud.net/Razrabotka/Struktura_papki_local/Svoj_kod.html#resenie-vs-modul)).
> **Решение команды (2026-09-21): совмещаем.** Вопрос «модулем или нет?» остаётся первым; модульный
> стандарт агентства — осознанная практика для переиспользуемого кода; для всего остального —
> [[pattern-local-solution-structure|структура решения по книге]]. Раздел «Когда НЕ модуль» ниже
> уточнён: раньше он отправлял код в `init.php`.

## Проблема и контекст
Нужна функциональность, которую предстоит **переиспользовать и массово ставить на коробки клиентов**.
Если разложить её по `/local` точечно, переносить и обновлять на десятках порталов тяжело и ненадёжно.

## Решение
Оформлять переиспользуемую функциональность **отдельным модулем** `vendor.module`. Модуль даёт:
- жизненный цикл `install`/`uninstall` (БД, события, агенты, права, опции) —
  [[recipe-module-structure-and-install|Структура модуля и установка]];
- версионирование и распространение — [[recipe-module-versioning-and-private-distribution|Версии и дистрибуция]];
- изоляцию **от ядра** (пространство имён `Vendor\Module`, без правки ядра —
  [[antipattern-box-core-modification|Правка ядра]]); для **набора** отраслевых модулей допустимы
  управляемые зависимости между ними — см. [[pattern-module-library-monorepo|Библиотека модулей агентства]].

## Когда модуль
- Функциональность **переиспользуется** на нескольких проектах/коробках.
- Нужна воспроизводимая установка и обновление у многих клиентов.
- Есть своя БД-структура, события, агенты, настройки **и** всё это нужно переносить между порталами
  (своя структура данных сама по себе ещё не повод — у решения для неё есть инсталлеры).

**Проверка книги** — это модуль, а не «решение, оформленное под модуль», если:
1. для переноса на другую установку достаточно скопировать папку модуля;
2. для обновления не нужны ручные действия (заменить файлы, выполнить код, создать инфоблоки).

## Когда НЕ модуль (достаточно `/local`)
- Код под одного клиента, который переносить не будем → **решение** в `/local/php_interface` по
  стандартной структуре: классы в `classes/`, подписки в `events.php`, окружение в `kernel.php`,
  инсталлеры в `install/` — [[pattern-local-solution-structure]]. В `init.php` — только автозагрузчик и
  подключение файлов, логики там нет.
- Маленький обработчик события, не требующий установки и переноса.
- Оверинжиниринг: не заворачивать в модуль каждую мелочь.

## `/local/modules` vs `/bitrix/modules`
- **`/local/modules/vendor.module`** — своё, не затирается обновлениями платформы. **Рекомендация по
  умолчанию** для приватных решений.
- **`/bitrix/modules/vendor.module`** — требуется для **тиражных решений marketplace.1c-bitrix.ru**
  (там свои правила упаковки и обновлений).

## Последствия
- **Плюсы:** воспроизводимая установка, обновляемость, изоляция, готовность к дистрибуции.
- **Минусы:** оверхед на структуру — не оправдан для разовых мелочей.

## Связанное
- [[pattern-local-solution-structure]] — ветка «не модуль»: структура решения по книге
- [[recipe-module-structure-and-install]], [[recipe-module-versioning-and-private-distribution]]
- [[concept-dev-standards|Стандарт разработки]]

[← Свои модули](_index-modules-custom.md)
