---
title: "CBPTaskService — задания бизнес-процессов"
type: entity
module: bizproc
edition: box
status: verified
provenance: mixed
verified: "2026-07-13 / коробка: классический попап заданий и новый UI в карточке смарт-процесса; 2026-09-22 / курс 57 dev.1c-bitrix.ru и код стенда (bizproc 26.1075.0): базовые классы штатных заданий, типы делегирования, выполнение задания из кода; apidocs.bitrix24.ru — bizproc.task.complete"
tags: [bizproc, задание, task, класс, делегирование]
sources: ["[[source-devbook-bizproc]]", "[[source-course57-basics]]", "[[source-course57-actions-core]]", "[[source-course57-developer]]"]
related: ["[[recipe-bizproc-custom-task-activity]]", "[[entity-cbp-activity]]", "[[concept-bizproc-engine]]", "[[concept-bizproc-activity-catalog]]", "[[entity-bizproc-template-rest-methods]]"]
aliases: ["bitrix24-cbptaskservice"]
updated: "2026-09-22"
---

# `CBPTaskService`

**Что это:** сервис движка БП для заданий — интерактивных шагов, которые ждут ответа человека.
Задание, созданное через него, попадает в штатный список заданий и в живую ленту.

> **Сверено с курсом 57 и ядром 2026-09-22.** Спор о базовом классе снят: в ядре штатны оба
> варианта. Подпись «`DELEGATION_TYPE` `0` — убрать кнопку «Делегировать»» была неверной: `0` —
> «только подчинённым». «Делегирование разрешено глобальной настройкой модуля» — не подтвердилось:
> тип делегирования задаёт само действие. Методы сервиса книга и курс не разбирают — они по-прежнему
> эмпирика команды.

## Ключевые факты
| Поле | Значение |
|------|----------|
| Тип | сервис движка |
| Модуль | `bizproc` |
| Получение | `$this->workflow->GetService('TaskService')` внутри активити |
| Требует | активити с `IBPEventActivity` + `IBPActivityExternalEventListener` (так и по книге) |
| Базовый класс задания | без веток — `CBPActivity` (штатное «Ознакомление»), с ветками «да/нет» — `CBPCompositeActivity` («Утверждение», «Запрос информации») — ядро bizproc 26.1075.0 |

## Методы, которые нужны на практике

| Метод | Когда вызывается |
|---|---|
| `CreateTask(array $fields): int` | в `Subscribe()` — создать задание |
| `MarkCompleted($taskId, $userId, CBPTaskUserStatus::Ok)` | в `OnExternalEvent()` — после ответа человека |
| `Update($taskId, ['STATUS' => …])` | в `Unsubscribe()`, если задание завершилось штатно |
| `DeleteTask($taskId)` | в `Unsubscribe()`, если процесс отменён |
| `GetList(...)` | получить данные задания, в том числе `DOCUMENT_ID` по `TASK_ID` |

Разделение в `Unsubscribe()` делается по внутреннему флагу: статус не выставлен — процесс
отменяют, задание надо **удалить**; выставлен — обновить.

## Важные поля `CreateTask`

| Поле | Смысл |
|---|---|
| `USERS` | список исполнителей (через `CBPHelper::ExtractUsers`) |
| `WORKFLOW_ID`, `ACTIVITY`, `ACTIVITY_NAME` | привязка к процессу и активити |
| `NAME`, `DESCRIPTION` | что человек видит в списке заданий |
| **`PARAMETERS`** | произвольные данные для формы задания |
| `DELEGATION_TYPE` | `CBPTaskDelegationType`: `0` — только подчинённым, `1` — всем сотрудникам, `2` — никому (`3` — служебный) |
| `OVERDUE_DATE` | срок задания в списке. Штатные задания пишут сюда момент истечения таймаута, а без таймаута — свойство `OverdueDate`; сам процесс это поле не двигает (код ядра) |
| `IS_INLINE` | режим отображения; штатное утверждение ставит `Y`, когда поле пояснения не показывается |
| `DOCUMENT_NAME` | название документа в списке |

**`PARAMETERS` — ключевой приём.** `ShowTaskForm()` и `PostTaskForm()` читают данные из
`$arTask['PARAMETERS']` и не обращаются к остановленному процессу. Альтернатива — перезагружать
workflow и доставать свойства активити — работает, но хрупка: в новом UI смарт-процессов может
вернуть пустые свойства, и человек увидит пустое задание.

## Кто и как выполняет задания
- Задания видят адресаты: пользователи, группы, отделы. Список — «Автоматизация → Бизнес-процессы →
  Процессы», фильтр «Требуют выполнения». **Администратор может выполнить чужое задание**, при
  удалении документа его задания удаляются
  ([урок 5276](https://dev.1c-bitrix.ru/learning/course/?COURSE_ID=57&LESSON_ID=5276)).
- Кнопка «Делегировать» есть при типах «Только подчиненным» и «Всем сотрудникам»; администратор
  делегирует и при запрете ([урок 3771](https://dev.1c-bitrix.ru/learning/course/?COURSE_ID=57&LESSON_ID=3771)).
- **Из PHP** — внешнее событие действию: для утверждения параметры `USER_ID` и `APPROVE`
  ([урок 5815](https://dev.1c-bitrix.ru/learning/course/?COURSE_ID=57&LESSON_ID=5815)). В текущем ядре
  `CBPDocument::SendExternalEvent($workflowId, $activityName, $params, &$errors)` требует четвёртый
  аргумент — вызов из урока с тремя упадёт; `CBPRuntime::SendExternalEvent()` принимает три.
- **Через REST** — `bizproc.task.complete`: `TASK_ID`, `STATUS` (`1`/`yes` — утверждено, `2`/`no` —
  отклонено, `3`/`ok` — ознакомлен, `4`/`cancel` — отмена; набор зависит от типа задания), `COMMENT`,
  `FIELDS` — поля запроса информации
  ([apidocs](https://apidocs.bitrix24.ru/api-reference/bizproc/bizproc-task/bizproc-task-complete.html)).

## Подводные камни

- **`ACTIVITY` должен совпадать с именем класса без префикса `CBP`.** Не совпало — задание не
  появится в попапе, без каких-либо ошибок.
- **Пробуждать процесс только статически:** `CBPRuntime::SendExternalEvent(...)`. Вызов как метода
  экземпляра не разбудит процесс.
- **Новый UI кладёт форму задания в `<div>`, а не в `<table>`.** Вёрстка `tasktemplate.php` с
  одинокими `<tr>` даст пустое тело задания при видимых кнопках.
- **Без `Cancel()` и `HandleFault()` в активити** задание останется висеть у людей после отмены
  процесса.
- **Делегирование задаёт действие, а не модуль.** У штатных заданий свойство `DelegationType`, в ядре
  по умолчанию `0` («только подчинённым»); запретить — `2` («никому»).

> **Изменено 2026-09-22.** Раньше здесь было «по умолчанию делегирование разрешено глобальной
> настройкой модуля; если мешает — `DELEGATION_TYPE => 0`». В настройках модуля делегирования нет
> (курс, урок 4678), а `0` по константам ядра — «только подчинённым».

## Открытые вопросы
- Чем именно отличается отображение при `IS_INLINE` `'Y'` и `'N'` — проверить в интерфейсе.

## Связанное
- [[recipe-bizproc-custom-task-activity]] — полный рецепт задания-активити
- [[entity-cbp-activity]] — класс, из которого вызывается сервис
- [[concept-bizproc-activity-catalog]] — штатные задания: сроки, результаты, что будет по истечении срока

[← Бизнес-процессы](_index-bizproc.md)
